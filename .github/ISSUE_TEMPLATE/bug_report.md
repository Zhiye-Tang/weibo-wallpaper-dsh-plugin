---
name: 🐛 Bug 报告
about: 报告一个让插件不工作或表现异常的问题
title: '[Bug] 一句话描述问题'
labels: bug
assignees: ''
---

感谢你花时间报告问题!请尽量填全下面信息,方便定位。

## 环境
- DSH 版本 / 启动方式(如 `dsh web`): ________
- 操作系统: ________
- Node 版本(如能确认): ________
- 插件版本 / 获取方式(克隆 commit、压缩包日期): ________
- profile 名称与 `patchReload` 配置(live/startup): ________

## 问题描述
发生了什么?预期是什么?

## 复现步骤
1. ________
2. ________
3. ________

## 日志 / 报错
- DSH 启动日志(含 `[zly-engine]` 前缀的行最有用):
```
粘贴日志
```
- 浏览器 Console 报错(若有):
```
粘贴报错
```

## 状态与文件(可选,不包含 Cookie!)
- `/zly-wallpaper/state` 返回的 JSON 关键字段(`ok`、`lastError`、`cookieNeeded`、`historical`):
```
粘贴(请先删除任何 Cookie 字段!)
```

> ⚠️ 隐私提醒: **永远不要**粘贴 `state.json` 全文或任何含 `Cookie:`/`SUB=` 的内容,不要粘贴本地根目录全路径。

## 截图
如有,请附上(壁纸控制条 / 报错画面)。
